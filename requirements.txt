# Server1-whisper 업데이트된 requirements.txt
# (기존 패키지들 + 개선된 메트릭 패키지)

fastapi==0.109.0
uvicorn==0.27.0
requests==2.31.0
faster-whisper
torch>=2.0.0
python-multipart==0.0.6
ffmpeg-python==0.2.0

# 배치 처리 및 추가 기능을 위한 패키지
numpy>=1.25.0
tqdm>=4.66.0
hf_xet

# 로그
python-json-logger

# ─── Prometheus 메트릭 (개선) ──────────────────────────────────
prometheus-fastapi-instrumentator>=7.0.0  # 추가: 자동 메트릭
prometheus_client>=0.20.0

# ─── GPU 모니터링 (통일) ──────────────────────────────────────
nvidia-ml-py3
